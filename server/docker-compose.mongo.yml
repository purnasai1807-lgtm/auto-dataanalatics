services:
  mongo:
    image: mongo:7
    container_name: insightforge-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - insightforge_mongo_data:/data/db

volumes:
  insightforge_mongo_data:
